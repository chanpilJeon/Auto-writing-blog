# Concept: 정보 → 블로그 자동 작성 프로그램

> 사용자가 정보(기사 링크, 텍스트, 메모, 영상 요약 등)를 던져주면,
> Claude가 이를 분석·재구성하여 블로그 글을 자동으로 작성하고 발행까지 해주는 프로그램.

---

## 1. 목표와 범위

### 핵심 목표
- **입력**: URL, 복사한 텍스트, 메모, 여러 자료 묶음 등 형식에 구애받지 않는 정보
- **출력**: 제목·본문·태그·카테고리가 갖춰진, 내 말투로 쓰인 블로그 글
- **자동화 수준**: 초안 생성 → (선택적 검토) → 발행까지 한 흐름으로

### 범위에서 제외할 것 (초기 버전)
- 이미지 자동 생성/삽입 (2차 단계로 미룸)
- 여러 블로그 플랫폼 동시 발행 (하나부터 시작)
- 예약 발행 스케줄링 (나중에 추가 용이)

---

## 2. 전체 아키텍처

```
[입력]                [처리 파이프라인]                       [출력]

URL/텍스트/메모  →  ① 수집·정제  →  ② 분석·구조화  →  ③ 초안 작성  →  ④ 다듬기·SEO  →  ⑤ 발행
                    (크롤링,          (핵심 추출,        (Claude API,      (제목 후보,       (플랫폼 API
                     본문 추출)        글감 각도 결정)     페르소나 적용)     태그, 요약)        or 파일 출력)
                                                              ↕
                                                      [사람 검토 단계] ← 선택적, 강력 추천
```

### 5단계 파이프라인 상세

| 단계 | 역할 | 구현 방법 |
|------|------|-----------|
| ① 수집·정제 | URL이면 본문 크롤링, 텍스트면 그대로. 광고/네비게이션 제거 | `requests` + `trafilatura`(본문 추출 특화) 또는 Claude에게 직접 정제 요청 |
| ② 분석·구조화 | 핵심 논점 추출, 글의 각도(정보 전달/의견/리뷰) 결정, 목차 설계 | Claude API 1차 호출 — "이 자료로 어떤 글을 쓸지" 기획 |
| ③ 초안 작성 | 목차 기반으로 본문 작성. 내 말투·스타일 프롬프트 적용 | Claude API 2차 호출 — 기획안 + 스타일 가이드 투입 |
| ④ 다듬기·SEO | 제목 후보 3~5개, 메타 설명, 태그, 소제목 키워드 최적화 | Claude API 3차 호출 (또는 ③과 통합) |
| ⑤ 발행 | 플랫폼에 업로드 or 마크다운 파일로 저장 | 플랫폼별 API / 파일 출력 |

**포인트**: 한 번의 거대한 프롬프트보다 **기획 → 작성 → 다듬기**로 호출을 나누는 편이 품질이 훨씬 좋다. 각 단계의 중간 산출물을 저장해두면 "기획은 좋은데 글이 별로"일 때 그 단계만 다시 돌릴 수 있다.

---

## 3. 발행 대상 플랫폼 선택 (중요한 선행 결정)

| 플랫폼 | 자동 발행 가능? | 비고 |
|--------|:---:|------|
| **WordPress** (설치형/wordpress.com) | ✅ | REST API 완비. 자동화에 가장 적합 |
| **Ghost** | ✅ | Admin API 제공. 개발자 친화적 |
| **GitHub Pages** (Jekyll/Hugo) | ✅ | 마크다운 파일을 git push하면 끝. 가장 단순 |
| **Medium** | ⚠️ | 신규 API 토큰 발급 중단됨 |
| **Tistory** | ❌ | Open API 2024년 종료 |
| **네이버 블로그** | ❌ | 공식 글쓰기 API 없음 (브라우저 자동화는 약관 위험) |

**추천 경로**:
- 이미 티스토리/네이버를 쓴다면 → **④단계까지 자동화하고 발행은 복붙** (마크다운/HTML 클립보드 복사 기능 제공)
- 새로 시작한다면 → **GitHub Pages** (완전 무료, git push = 발행) 또는 **WordPress**

---

## 4. 기술 스택 제안

### 옵션 A — Python 스크립트/CLI (추천 시작점)
```
Python 3.11+
├── anthropic          # Claude API SDK
├── trafilatura        # URL 본문 추출
├── python-frontmatter # 마크다운 메타데이터
└── click 또는 typer   # CLI 인터페이스
```
- 구조가 단순하고 디버깅이 쉬움. MVP에 최적.
- 사용 예: `blog write "https://example.com/article" --style casual`

### 옵션 B — Claude Agent SDK 기반 에이전트
- 크롤링 실패 시 검색으로 보완, 여러 자료 교차 검증 등 **판단이 필요한 작업**을 에이전트가 알아서 수행.
- 옵션 A로 파이프라인을 검증한 뒤 업그레이드하는 경로로 적합.

### 옵션 C — Claude Code 스킬(Skill)로 구현
- 별도 프로그램 없이, 지금 쓰는 Claude Code에 `/blog` 스킬을 만들어 "이 자료로 블로그 글 써줘" 하면 파일 생성까지 수행.
- **개발 비용이 가장 낮음.** 이미 콜드필 아카이브 워크플로우(정보 → Notion 기록)가 있으므로, "아카이브된 정보 → 블로그 글" 연계도 자연스러움.

> 💡 현실적 추천: **옵션 C로 1주 안에 굴려보고**, 쓸만하다 싶으면 옵션 A로 독립 프로그램화.

---

## 5. 품질의 핵심: 스타일 가이드 (프롬프트 설계)

자동 생성 글이 "AI 티 나는 글"이 되지 않으려면 **내 스타일 정의 파일**이 필수다.

`style-guide.md` 예시 구조:
```markdown
## 말투
- 해요체 / ~다체 (선택)
- 독자를 "여러분"이 아닌 2인칭 생략으로

## 글 구조 선호
- 도입부는 질문이나 구체적 사례로 시작 (일반론 금지)
- 소제목은 3~5개, 각 섹션 300~600자
- 마지막은 요약이 아니라 내 의견/전망으로 마무리

## 금지 사항
- "결론적으로", "요약하자면" 같은 상투어
- 과도한 이모지, 불릿 남발
- 원문 표현 그대로 옮기기 (반드시 재서술)

## 좋은 예시 (내가 쓴 글 2~3편 붙여넣기)
...
```

**내가 실제로 쓴 글 2~3편을 예시로 넣는 것**이 어떤 규칙 나열보다 효과가 크다 (few-shot 학습).

---

## 6. 구현 로드맵

### Phase 1 — MVP (1~2일)
- [ ] 발행 플랫폼 결정
- [ ] `style-guide.md` 작성 (기존 글 샘플 수집)
- [ ] 텍스트 입력 → Claude API → 마크다운 파일 출력 (파이프라인 ②③④)
- [ ] 결과물 10편 정도 뽑아보며 스타일 가이드 튜닝

### Phase 2 — 입력 확장 (2~3일)
- [ ] URL 입력 지원 (본문 크롤링)
- [ ] 여러 자료를 한 글로 종합하는 모드
- [ ] 기획안 검토 단계 추가 (목차를 먼저 보여주고 승인 후 작성)

### Phase 3 — 발행 자동화 (2~3일)
- [ ] 플랫폼 API 연동 (초안(draft) 상태로 업로드 → 사람이 최종 발행 버튼)
- [ ] 카테고리/태그 자동 매핑

### Phase 4 — 고도화 (선택)
- [ ] 콜드필 아카이브(Notion) 연동: 아카이브된 항목을 골라 블로그 글로 변환
- [ ] 이미지 생성/삽입, 예약 발행, 시리즈 글 관리

---

## 7. 운영 시 유의사항

1. **완전 무인 발행은 비추천.** 사실 오류(환각), 저작권 문제가 섞여 나갈 수 있다. "초안 자동 생성 + 발행 전 1분 검토"가 현실적 최적점.
2. **저작권**: 원문 요약·재구성은 괜찮지만, 원문 문장을 그대로 옮기면 안 된다. 프롬프트에 "재서술 필수" 명시 + 출처 링크 표기.
3. **비용**: 글 1편 = API 호출 3회, 대략 입력 1만~3만 토큰 수준. Sonnet 기준 편당 수십 원 정도로 부담 없음. 품질이 아쉬우면 작성 단계만 Opus로.
4. **검색엔진 관점**: AI 대량 생성 글은 검색 노출에 불리할 수 있다. 양산보다 "내 관점이 들어간 글을 빠르게 쓰는 도구"로 쓰는 게 장기적으로 유리.

---

## 8. 다음 액션

1. 발행 플랫폼 결정 (현재 쓰는 블로그가 있는지 → 그에 맞춰 ⑤단계 설계)
2. 내가 쓴 글 2~3편으로 `style-guide.md` 초안 만들기
3. 옵션 C(Claude Code 스킬) 또는 옵션 A(Python CLI)로 Phase 1 착수